# Contributing

Contributions are welcome. Open an issue before substantial changes, keep pull requests focused, and add tests for behavioral changes.

Before opening a pull request, run:

```bash
npm run typecheck
npm run lint
npm test
npm run build
```

Never commit API keys, production URLs containing secrets, or user data.
